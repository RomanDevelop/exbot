# 🚀 Быстрые команды для запуска PolExamBot

## Первый запуск (однажды)

```bash
# 1. Создать виртуальное окружение
python3 -m venv venv

# 2. Активировать виртуальное окружение
source venv/bin/activate

# 3. Установить зависимости
pip install -r requirements.txt

# 4. Настроить .env файл (вставить токен и Chat ID)
# Откройте .env и заполните данные
```

## Обычный запуск (каждый раз)

### Вариант 1: Простой скрипт (рекомендуется) ⭐
```bash
./start.sh
```

### Вариант 2: С активацией виртуального окружения
```bash
source venv/bin/activate
python pol_exam_bot.py
```

### Вариант 3: Без активации (прямой запуск)
```bash
venv/bin/python pol_exam_bot.py
```

## Проверка работы

```bash
# Проверить, запущен ли бот
ps aux | grep pol_exam_bot.py

# Посмотреть текущий статус
cat exam_status.json

# Проверить подключение к Telegram
source venv/bin/activate
python test_bot.py
```

## Остановка бота

```bash
# Остановить бота
pkill -f pol_exam_bot.py

# Или найти и остановить вручную
ps aux | grep pol_exam_bot.py
kill <PID>
```

## Важные замечания

✅ **Виртуальное окружение уже создано** - просто используйте `./start.sh` или активируйте `venv`

✅ **Бот работает в фоновом режиме** - он продолжит работать даже если закрыть терминал

✅ **Не забудьте отправить `/start` боту @PolExamBot в Telegram** для получения уведомлений

