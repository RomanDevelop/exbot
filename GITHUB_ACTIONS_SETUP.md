# 🚀 Настройка GitHub Actions для PolExamBot

## Быстрая настройка (5 минут)

### 1. Создайте репозиторий на GitHub

Если у вас еще нет репозитория:
```bash
git init
git add .
git commit -m "Initial commit: PolExamBot"
git branch -M main
git remote add origin https://github.com/ВАШ_USERNAME/ВАШ_РЕПОЗИТОРИЙ.git
git push -u origin main
```

### 2. Добавьте секреты в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите в `Settings` → `Secrets and variables` → `Actions`
3. Нажмите `New repository secret` и добавьте три секрета:

   **TELEGRAM_TOKEN:**
   ```
   8546079326:AAHTvXAfNKGzaxwHjQmZ7iicdjn9kDFTVfY
   ```

   **TELEGRAM_CHAT_ID:**
   ```
   1403891742
   ```

   **CHECK_URL:**
   ```
   https://chowaniak-school.pl/page/egzamin-certyfikatowy
   ```

### 3. Проверьте файлы

Убедитесь, что в репозитории есть:
- ✅ `.github/workflows/check.yml`
- ✅ `check_and_notify.py`
- ✅ `exam_status.json`

### 4. Запустите workflow вручную

1. Перейдите в `Actions` в вашем репозитории
2. Выберите `PolExamBot scheduled check`
3. Нажмите `Run workflow` → `Run workflow`
4. Дождитесь выполнения (обычно 10-30 секунд)

### 5. Проверьте результат

- ✅ Workflow должен выполниться успешно
- ✅ Если статус изменился, вы получите уведомление в Telegram
- ✅ Файл `exam_status.json` обновится в репозитории

## Расписание запуска

По умолчанию workflow запускается **каждые 30 минут**.

### Изменение расписания

Откройте файл `.github/workflows/check.yml` и измените строку:

```yaml
schedule:
  - cron: '*/30 * * * *'   # каждые 30 минут
```

**Примеры расписания:**

```yaml
# Каждые 15 минут
- cron: '*/15 * * * *'

# Каждые 2 часа
- cron: '0 */2 * * *'

# Каждый час в рабочие дни (9:00-18:00)
- cron: '0 9-18 * * 1-5'

# Раз в день в 9:00 утра
- cron: '0 9 * * *'
```

**Формат cron:** `минуты часы день месяц день_недели`

## Мониторинг

### Просмотр логов

1. Перейдите в `Actions`
2. Выберите последний запуск
3. Нажмите на `check` job
4. Просмотрите логи выполнения

### Просмотр статуса

Последний статус хранится в файле `exam_status.json` в репозитории:
```json
{"status": "closed"}
```

Возможные значения:
- `open` - регистрация открыта
- `closed` - регистрация закрыта
- `unknown` - статус не определен
- `error` - ошибка при проверке

## Устранение проблем

### Workflow не запускается

1. Проверьте, что файл `.github/workflows/check.yml` находится в репозитории
2. Убедитесь, что файл закоммичен в ветку `main` или `master`
3. Проверьте синтаксис YAML файла

### Не приходят уведомления

1. Проверьте, что секреты добавлены правильно
2. Убедитесь, что вы отправили `/start` боту @PolExamBot в Telegram
3. Проверьте Chat ID в секретах (должен быть правильный)
4. Посмотрите логи workflow - там будут ошибки, если они есть

### Ошибка "Chat not found"

1. Откройте Telegram
2. Найдите бота @PolExamBot
3. Отправьте команду `/start`
4. Запустите workflow снова

### Ошибка при push

1. Убедитесь, что в `.github/workflows/check.yml` есть `persist-credentials: true`
2. Проверьте, что у workflow есть права на запись в репозиторий
3. Посмотрите логи выполнения

## Преимущества GitHub Actions

✅ **Бесплатно** - до 2000 минут/месяц для публичных репозиториев
✅ **Надежно** - автоматический запуск по расписанию
✅ **Без хостинга** - не нужно держать сервер включенным
✅ **Простота** - настройка за 5 минут
✅ **Мониторинг** - логи всех запусков доступны в GitHub

## Отключение GitHub Actions

Если хотите отключить автоматический запуск:

1. Удалите файл `.github/workflows/check.yml`
2. Или закомментируйте секцию `schedule:` в файле

Для запуска вручную используйте `workflow_dispatch` (уже настроено).

---

**Готово!** 🎉 Теперь ваш бот будет работать 24/7 без вашего участия!

