# 🤖 PolExamBot

Telegram-бот для мониторинга статуса регистрации на экзамены по польскому языку.

## 📋 Описание

PolExamBot автоматически проверяет сайт экзаменов и отправляет уведомления в Telegram при изменении статуса регистрации (открыта/закрыта).

## 🚀 Быстрый старт

### 1. Создание виртуального окружения (если еще не создано)

```bash
python3 -m venv venv
```

### 2. Активация виртуального окружения и установка зависимостей

**На macOS/Linux:**

```bash
source venv/bin/activate
pip install -r requirements.txt
```

**На Windows:**

```cmd
venv\Scripts\activate
pip install -r requirements.txt
```

### 3. Настройка конфигурации

Откройте файл `.env` и заполните следующие параметры:

```env
TELEGRAM_TOKEN=ваш_токен_бота
TELEGRAM_CHAT_ID=ваш_chat_id
CHECK_URL=https://chowaniak-school.pl/page/egzamin-certyfikatowy
```

#### Как получить Telegram токен:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте полученный токен
4. Вставьте токен в `.env` файл

#### Как получить Chat ID:

**Для личных сообщений:**

1. Найдите бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему `/start`
3. Скопируйте ваш `Id` (число вида `123456789`)
4. Вставьте в `.env` файл

**Для канала:**

1. Добавьте бота в канал как администратора
2. Отправьте сообщение в канал
3. Перейдите по ссылке: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
4. Найдите `chat` → `id` в ответе (будет отрицательное число вида `-1001234567890`)
5. Вставьте в `.env` файл

### 3.5. Проверка подключения (рекомендуется)

После настройки `.env` файла рекомендуется проверить подключение:

```bash
python test_bot.py
```

Этот скрипт:

- ✅ Проверит, что токен найден и работает
- ✅ Покажет информацию о вашем боте
- ✅ Отправит тестовое сообщение (если Chat ID установлен)
- ✅ Поможет получить Chat ID, если он еще не установлен

**Если вы видите ошибку с Chat ID:**

1. Для личного чата: отправьте боту `/start` в Telegram
2. Для канала: добавьте бота как администратора в канал

### 4. Запуск бота

**Вариант 1: Используя скрипт (рекомендуется)**

```bash
./start.sh
```

**Вариант 2: Вручную (с активацией виртуального окружения)**

```bash
source venv/bin/activate  # Активировать виртуальное окружение
python pol_exam_bot.py
```

**Вариант 3: Без активации виртуального окружения**

```bash
venv/bin/python pol_exam_bot.py
```

Бот начнет проверять сайт каждые 2 часа и уведомит вас при изменении статуса.

**Примечание:** Бот будет работать в фоновом режиме. Для остановки используйте `Ctrl+C` или команду:

```bash
pkill -f pol_exam_bot.py
```

## 📁 Структура проекта

```
PolExamBot/
├── pol_exam_bot.py      # Основной код бота
├── test_bot.py          # Скрипт для проверки подключения
├── start.sh             # Скрипт для запуска бота
├── requirements.txt     # Зависимости проекта
├── venv/                # Виртуальное окружение Python
├── .env                 # Конфигурация (не коммитится в git)
├── .gitignore          # Игнорируемые файлы
├── exam_status.json    # Последний сохраненный статус (создается автоматически)
└── README.md           # Этот файл
```

## ⚙️ Как это работает

1. Бот каждые 2 часа отправляет HTTP-запрос на указанный URL
2. Парсит HTML-страницу и ищет ключевые фразы:
   - `"Zapisy otwarte"` или `"Rejestracja otwarta"` → статус **open**
   - `"Brak miejsc"` или `"Zamknięta"` → статус **closed**
3. Сравнивает текущий статус с предыдущим (из `exam_status.json`)
4. Если статус изменился → отправляет уведомление в Telegram
5. Сохраняет новый статус в файл

## 🔧 Настройка расписания

По умолчанию бот проверяет сайт каждые 2 часа. Чтобы изменить интервал, отредактируйте строку в `pol_exam_bot.py`:

```python
schedule.every(2).hours.do(check_updates)  # Каждые 2 часа
# или
schedule.every(30).minutes.do(check_updates)  # Каждые 30 минут
# или
schedule.every().day.at("09:00").do(check_updates)  # Каждый день в 9:00
```

## 🌐 Развертывание на сервере

Для круглосуточной работы бота можно развернуть его на:

### Render

1. Создайте новый Web Service
2. Подключите репозиторий GitHub/GitLab
3. Добавьте переменные окружения из `.env`
4. Установите команду запуска: `python pol_exam_bot.py`

### Railway

1. Создайте новый проект
2. Добавьте GitHub репозиторий
3. Настройте переменные окружения
4. Railway автоматически определит Python проект

### VPS (Ubuntu/Linux)

1. Установите Python и зависимости:

   ```bash
   sudo apt update
   sudo apt install python3 python3-pip
   pip3 install -r requirements.txt
   ```

2. Создайте systemd сервис (`/etc/systemd/system/polexambot.service`):

   ```ini
   [Unit]
   Description=PolExamBot
   After=network.target

   [Service]
   Type=simple
   User=your_user
   WorkingDirectory=/path/to/PolExamBot
   ExecStart=/usr/bin/python3 pol_exam_bot.py
   Restart=always

   [Install]
   WantedBy=multi-user.target
   ```

3. Запустите сервис:
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable polexambot
   sudo systemctl start polexambot
   ```

## 🛠 Возможные улучшения

- ✅ Добавить логирование в файл (`logging` модуль)
- ✅ Поддержка нескольких сайтов для мониторинга
- ✅ Веб-интерфейс для управления настройками (Flask/FastAPI)
- ✅ Уведомления о критических ошибках
- ✅ Статистика проверок и изменений статуса

## ⚠️ Важные замечания

1. **Безопасность:** Никогда не публикуйте файл `.env` с токенами в публичных репозиториях
2. **Ограничения Telegram API:** Не делайте слишком частые проверки (< 1 минуты), чтобы не заблокировать бота
3. **Стабильность сети:** Убедитесь, что сервер/компьютер имеет стабильное интернет-соединение

## 📝 Лицензия

Этот проект создан в образовательных целях. Используйте на свое усмотрение.

## 🆘 Решение проблем

**Бот не отправляет сообщения:**

- Проверьте правильность токена и Chat ID в `.env`
- Убедитесь, что бот добавлен в канал как администратор (если используете канал)

**Ошибки при парсинге:**

- Сайт может изменить структуру HTML - обновите поиск фраз в функции `get_exam_status()`

**Бот не запускается:**

- Проверьте установку всех зависимостей: `pip install -r requirements.txt`
- Убедитесь, что используете Python 3.7+

---

**Удачного мониторинга! 🎓**
# exbot
